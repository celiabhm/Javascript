// Créer une fonction qui calcul la moyenne de 2 notes //

let noteEps=12;
let noteMath=3; 

function moyenneNotes(note1,note2){
    return (note1 + note2)/2;
};

console.log(moyenneNotes(noteEps,noteMath));
